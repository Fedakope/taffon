<%= link_to t('notifications.clean_all'), notifications.clean_notifications_path, class: 'btn btn-sm btn-secondary text-left', method: 'delete' %>

<% @notifications.each do |notification| %>
  <%= render 'shared/notification-card', notification: notification %>
<% end %>
