  <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://www.redsundyechem.com/wp-content/uploads/2017/03/events-banner.jpg;">
    <div class="container">
      <div class="banner-content">
        <%= render "shared/search", path: organizer_events_path %>
      </div>
    </div>
  </div>

<h1>List of Events</h1>
<div class="grid-container">
  <div class="events-cards">
        <% @events.each do |event| %>
          <div class="col-xs-12 col-sm-6 col-md-4 card-trip">
            <img src="https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/greece.jpg">
            <div class="card-trip-infos">
              <div>
                <% if event.jobs.present? %>
                  <%= link_to organizer_event_path(event) do %>
                    <h2><%= event.name %></h2>
                    <p><%= event.description %></p>
                  <% end %>
                <% end %>
              </div>
              <h2 class="card-trip-pricing"><%= event.jobs.count %> available jobs</h2>
            </div>
          </div>
        <% end %>
  </div>

  <!-- Map container -->
  <div
    class="map"
    id="map"
    style="width: 100%;
    height: 400px;"
    data-markers="<%= @markers.to_json %>"
    data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
  ></div>

  <!-- Map container -->
  <div class="featured-job">
    <div class="notification">
      <div class="notification-content">
        <p><small>14th January</small></p>
        <p>Lorem ipsum dolor sit amet, <strong>consectetur</strong> adipisicing elit.</p>
      </div>

      <div class="notification-actions">
        <a href="#">Edit <i class="fas fa-pencil-alt"></i></a>
        <a href="#">Delete <i class="far fa-trash-alt"></i></a>
      </div>
    </div>

    <div class="notification">
      <div class="notification-content">
        <p><small>9th January</small></p>
        <p>Lorem ipsum dolor sit amet, <strong>consectetur</strong> adipisicing elit.</p>
      </div>

      <div class="notification-actions">
        <a href="#">Edit <i class="fas fa-pencil-alt"></i></a>
        <a href="#">Delete <i class="far fa-trash-alt"></i></a>
      </div>
    </div>
  </div>
  <!-- Map container -->
  <div class="job-category">

  </div>
</div>